# LVGL UI Implementation Plan — Waveshare ESP32-S3-Touch-LCD-4B

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the color-test LVGL page with a full intercom/VA/weather UI on the 480x480 ST7701 display.

**Architecture:** Hybrid — 4 system overlay pages + 1 main_page with mutually exclusive groups (tileview for idle/weather, VA groups, IC groups). `draw_display` script handles all state-driven page/group switching.

**Tech Stack:** ESPHome LVGL, Figtree/Roboto/MDI fonts, PCF85063 RTC, HA weather service calls.

---

## File Modified

- `waveshare-s3-touch-4b-va-intercom.yaml` — ALL changes in this single file.

---

## Task 1: Add fonts and remove unused globals

**Files:**
- Modify: `waveshare-s3-touch-4b-va-intercom.yaml:75-76` (substitutions)
- Modify: `waveshare-s3-touch-4b-va-intercom.yaml:476-516` (globals — remove circular layout vars)
- Modify: `waveshare-s3-touch-4b-va-intercom.yaml:902-934` (fonts — add new fonts)

- [ ] **Step 1: Update substitutions for new font glyphsets**

Replace line 75-76 with:
```yaml
  font_glyphsets: "GF_Latin_Core"
  font_glyphsets_extra: "GF_Latin_Core"
  font_family: Figtree
```

- [ ] **Step 2: Remove unused circular layout globals**

Remove lines 497-516 (the globals `reply_page_index`, `reply_total_pages`, `wrapped_lines_reply`, `chord_widths_cache`, `text_row_y`, `y_metrics`). Keep only `pca9554_output_state`, `voice_assistant_phase`, `current_mode`, `previous_mode`, `peer_name`.

- [ ] **Step 3: Remove `compute_layout_metrics` script**

Remove lines 593-635 (the entire `compute_layout_metrics` script block).

- [ ] **Step 4: Clean up `va_end_cleanup` to remove circular layout refs**

Replace the `va_end_cleanup` extension (lines 522-530) with:
```yaml
  - id: !extend va_end_cleanup
    then:
      - globals.set:
          id: voice_assistant_phase
          value: '${voice_assist_idle_phase_id}'
```

- [ ] **Step 5: Add new fonts after existing `font_info` (after line 934)**

Append these font definitions:
```yaml
  - file:
      type: gfonts
      family: Figtree
      weight: 600
    id: font_btn
    size: 22
    glyphsets:
      - ${font_glyphsets_extra}
  - file:
      type: gfonts
      family: Roboto Mono
      weight: 400
    id: font_clock
    size: 48
    glyphs: '0123456789: '
  - file: materialsdesignicons.ttf
    id: font_icon
    size: 48
    glyphs: [
      "\U000F0599", "\U000F0594", "\U000F0590", "\U000F0595",
      "\U000F0591", "\U000F0597", "\U000F0598", "\U000F0593",
      "\U000F0592", "\U000F059D", "\U000F0596", "\U000F067F",
      "\U000F059E", "\U000F067E", "\U000F0F37",
      "\U000F0718", "\U000F071A", "\U000F091E", "\U000F091F",
      "\U000F0920", "\U000F0921", "\U000F0922",
      "\U000F05A9"
    ]
  - file: materialsdesignicons.ttf
    id: font_icon_sm
    size: 24
    glyphs: [
      "\U000F05A9", "\U000F05AA",
      "\U000F0599", "\U000F0594", "\U000F0590", "\U000F0595",
      "\U000F0591", "\U000F0597", "\U000F0598", "\U000F0593",
      "\U000F0592", "\U000F059D", "\U000F0596", "\U000F067F",
      "\U000F059E", "\U000F067E", "\U000F0F37"
    ]
```

Note: MDI TTF is bundled with ESPHome. The glyphs include all weather condition icons plus wifi/wifi-off icons.

- [ ] **Step 6: Verify the config compiles**

Run: `docker run --rm -v /mnt/services/esphome/esphome:/config -v /mnt/services/esphome/esphome-intercom:/config/intercom ghcr.io/esphome/esphome:2026.4.3 run --no-logging esp32-s3-smart-86-box-hall.yaml 2>&1 | tail -20`

Expected: Compilation should succeed (fonts download and compile).

- [ ] **Step 7: Commit**

```bash
git add waveshare-s3-touch-4b-va-intercom.yaml
git commit -m "feat(4b): add fonts and remove unused circular layout globals"
```

---

## Task 2: Add time/RTC and weather data infrastructure

**Files:**
- Modify: `waveshare-s3-touch-4b-va-intercom.yaml` — add `time:`, `pcf85063`, weather globals, forecast script

- [ ] **Step 1: Add PCF85063 RTC and SNTP time source**

Insert after the `sensor:` section (before `text_sensor:`):
```yaml
time:
  - platform: pcf85063
    id: rtc_time
    i2c_id: internal_i2c
    address: 0x51
  - platform: sntp
    id: sntp_time
    on_time_sync:
      - lambda: |-
          id(rtc_time).write_now();
```

- [ ] **Step 2: Add weather forecast globals**

Add to the `globals:` block:
```yaml
  - id: weather_condition
    type: std::string
    restore_value: no
    initial_value: '""'
  - id: weather_temperature
    type: float
    restore_value: no
    initial_value: "0.0"
  - id: weather_humidity
    type: float
    restore_value: no
    initial_value: "0.0"
  - id: weather_wind_speed
    type: float
    restore_value: no
    initial_value: "0.0"
  - id: forecast_condition
    type: std::string[5]
    restore_value: no
  - id: forecast_temp_max
    type: float[5]
    restore_value: no
  - id: forecast_temp_min
    type: float[5]
    restore_value: no
  - id: forecast_dow
    type: int[5]
    restore_value: no
```

- [ ] **Step 3: Add weather fetch and display scripts**

Add to `script:` block:
```yaml
  - id: fetch_weather
    mode: single
    then:
      - homeassistant.action:
          action: weather.get_forecasts
          data:
            entity_id: weather.home
            type: daily
          capture_response: true
          on_success:
            - lambda: |-
                auto now = id(sntp_time).now();
                if (now.is_valid()) {
                  // Current conditions from HA weather entity
                  // (the forecast response doesn't include current conditions,
                  //  we'll get those from weather.home sensor)
                }
                JsonObjectConst resp = response["response"];
                JsonObjectConst data = resp["weather.home"];
                JsonArrayConst forecast = data["forecast"];
                for (int i = 0; i < 5 && i < (int)forecast.size(); i++) {
                  JsonObjectConst day = forecast[i];
                  id(forecast_condition)[i] = day["condition"].as<std::string>();
                  id(forecast_temp_max)[i]  = day["temperature"]   | 0.0f;
                  id(forecast_temp_min)[i]  = day["templow"]       | 0.0f;
                  std::string dt = day["datetime"].as<std::string>();
                  int y, m, d;
                  sscanf(dt.c_str(), "%d-%d-%d", &y, &m, &d);
                  struct tm tm = {};
                  tm.tm_year = y - 1900;
                  tm.tm_mon = m - 1;
                  tm.tm_mday = d;
                  mktime(&tm);
                  id(forecast_dow)[i] = tm.tm_wday;
                }
            - script.execute: update_weather_display

  - id: update_weather_display
    mode: restart
    then:
      - lambda: |-
          auto icon_for = [](const std::string& c) -> const char* {
            if (c == "sunny") return "\U000F0599";
            if (c == "clear-night") return "\U000F0594";
            if (c == "cloudy") return "\U000F0590";
            if (c == "partlycloudy") return "\U000F0595";
            if (c == "fog") return "\U000F0591";
            if (c == "rainy") return "\U000F0597";
            if (c == "snowy") return "\U000F0598";
            if (c == "lightning") return "\U000F0593";
            if (c == "hail") return "\U000F0592";
            if (c == "windy") return "\U000F059D";
            if (c == "pouring") return "\U000F0596";
            if (c == "snowy-rainy") return "\U000F067F";
            if (c == "windy-variant") return "\U000F059E";
            if (c == "lightning-rainy") return "\U000F067E";
            if (c == "exceptional") return "\U000F0F37";
            return "\U000F0590";
          };
          auto text_for = [](const std::string& c) -> const char* {
            if (c == "sunny") return "Ясно";
            if (c == "clear-night") return "Ясно";
            if (c == "cloudy") return "Облачно";
            if (c == "partlycloudy") return "Переменная облачность";
            if (c == "fog") return "Туман";
            if (c == "rainy") return "Дождь";
            if (c == "snowy") return "Снег";
            if (c == "lightning") return "Гроза";
            if (c == "hail") return "Град";
            if (c == "windy") return "Ветрено";
            if (c == "pouring") return "Ливень";
            if (c == "snowy-rainy") return "Мокрый снег";
            if (c == "windy-variant") return "Ветрено";
            if (c == "lightning-rainy") return "Гроза";
            if (c == "exceptional") return "Особые условия";
            return "";
          };
          static const char* day_short_ru[] = {"Вс","Пн","Вт","Ср","Чт","Пт","Сб"};

          if (auto now = id(sntp_time).now(); now.is_valid()) {
            char clk[8];
            snprintf(clk, sizeof(clk), "%02d:%02d", now.hour, now.minute);
            lv_label_set_text(id(clock_label), clk);
          }
          lv_label_set_text(id(weather_icon_label), icon_for(id(weather_condition)));
          if (!std::isnan(id(weather_temperature))) {
            char temp[16];
            snprintf(temp, sizeof(temp), "%.0f\u00B0C", id(weather_temperature));
            lv_label_set_text(id(weather_temp_label), temp);
          }
          lv_label_set_text(id(weather_condition_label), text_for(id(weather_condition)));
          {
            char details[64];
            snprintf(details, sizeof(details), "\U000F0718 %.0f%%  \U000F059D %.0f км/ч",
                     id(weather_humidity), id(weather_wind_speed));
            lv_label_set_text(id(weather_details_label), details);
          }

          lv_obj_t* fc_day_labels[] = {id(fc0_day_label), id(fc1_day_label), id(fc2_day_label), id(fc3_day_label), id(fc4_day_label)};
          lv_obj_t* fc_icon_labels[] = {id(fc0_icon_label), id(fc1_icon_label), id(fc2_icon_label), id(fc3_icon_label), id(fc4_icon_label)};
          lv_obj_t* fc_max_labels[] = {id(fc0_max_label), id(fc1_max_label), id(fc2_max_label), id(fc3_max_label), id(fc4_max_label)};
          lv_obj_t* fc_min_labels[] = {id(fc0_min_label), id(fc1_min_label), id(fc2_min_label), id(fc3_min_label), id(fc4_min_label)};
          for (int i = 0; i < 5; i++) {
            int dow = id(forecast_dow)[i];
            lv_label_set_text(fc_day_labels[i], day_short_ru[dow]);
            auto& fc = id(forecast_condition)[i];
            lv_label_set_text(fc_icon_labels[i], icon_for(fc));
            char t[16];
            snprintf(t, sizeof(t), "%.0f\u00B0", id(forecast_temp_max)[i]);
            lv_label_set_text(fc_max_labels[i], t);
            snprintf(t, sizeof(t), "%.0f\u00B0", id(forecast_temp_min)[i]);
            lv_label_set_text(fc_min_labels[i], t);
          }
```

- [ ] **Step 4: Add weather interval trigger and HA sensor bindings**

Add to `api:` `on_client_connected` section:
```yaml
api:
  on_client_connected:
    - logger.log: "API client connected"
    - script.execute: fetch_weather
    - interval:
        interval: 10min
        then:
          - script.execute: fetch_weather
```

- [ ] **Step 5: Commit**

```bash
git add waveshare-s3-touch-4b-va-intercom.yaml
git commit -m "feat(4b): add RTC, weather data infrastructure"
```

---

## Task 3: Add system overlay pages (LVGL)

**Files:**
- Modify: `waveshare-s3-touch-4b-va-intercom.yaml:992-1013` (replace LVGL section)

- [ ] **Step 1: Replace the LVGL section — add system overlay pages**

Replace the entire `lvgl:` block (lines 984-1013) with system overlay pages only (main_page stays as stub with just bg_color for now):

```yaml
lvgl:
  displays:
    - main_display
  touchscreens:
    - touch
  buffer_size: 10%
  color_depth: 16
  log_level: WARN
  pages:
    - id: initializing_page
      bg_color: 0x000000
      widgets:
        - label:
            align: CENTER
            text: "Загрузка..."
            text_color: 0x8B949E
            text_font: font_state

    - id: no_wifi_page
      bg_color: 0x000000
      widgets:
        - label:
            align: TOP_MID
            y: 120
            text: "Нет WiFi"
            text_color: 0xE74C3C
            text_font: font_state
        - label:
            align: TOP_MID
            y: 180
            text: "Ожидание подключения..."
            text_color: 0x8B949E
            text_font: font_text

    - id: no_ha_page
      bg_color: 0x000000
      widgets:
        - label:
            align: TOP_MID
            y: 120
            text: "Нет Home Assistant"
            text_color: 0xF39C12
            text_font: font_state
        - label:
            align: TOP_MID
            y: 180
            text: "Ожидание API..."
            text_color: 0x8B949E
            text_font: font_text

    - id: timer_finished_page
      bg_color: 0x000000
      widgets:
        - label:
            align: CENTER
            y: -60
            text: "Таймер!"
            text_color: 0xF39C12
            text_font: font_state
        - button:
            align: CENTER
            y: 40
            width: 200
            height: 60
            bg_color: 0xE74C3C
            radius: 10
            on_click:
              - switch.turn_off: timer_ringing
            widgets:
              - label:
                  align: CENTER
                  text: "Стоп"
                  text_color: 0xE6EDF3
                  text_font: font_btn

    - id: main_page
      bg_color: 0x0D1117
```

- [ ] **Step 2: Verify config compiles**

Expected: Should compile with 5 pages defined (main_page is empty).

- [ ] **Step 3: Commit**

```bash
git add waveshare-s3-touch-4b-va-intercom.yaml
git commit -m "feat(4b): add system overlay LVGL pages"
```

---

## Task 4: Build main_page — top_bar + tileview (idle + weather tiles)

**Files:**
- Modify: `waveshare-s3-touch-4b-va-intercom.yaml` — replace `main_page` stub

- [ ] **Step 1: Replace the empty `main_page` with full layout**

Replace `- id: main_page` and its `bg_color: 0x0D1117` with:

```yaml
    - id: main_page
      bg_color: 0x0D1117
      widgets:
        # === TOP BAR ===
        - obj:
            id: top_bar
            width: 480
            height: 40
            align: TOP_MID
            bg_opa: TRANSP
            widgets:
              - label:
                  id: clock_label
                  align: TOP_LEFT
                  x: 10
                  y: 5
                  text: "--:--"
                  text_color: 0xE6EDF3
                  text_font: font_clock
              - label:
                  id: top_bar_peer
                  align: CENTER
                  text: "Home Assistant"
                  text_color: 0x8B949E
                  text_font: font_title
              - label:
                  id: wifi_icon_label
                  align: TOP_RIGHT
                  x: -10
                  y: 10
                  text: "\U000F05A9"
                  text_color: 0x8B949E
                  text_font: font_icon_sm

        # === TILEVIEW (idle + weather) ===
        - tileview:
            id: top_tileview
            width: 480
            height: 440
            align: TOP_MID
            y: 40
            bg_opa: TRANSP
            tiles:
              - row: 0
                column: 0
                dir: HOR
                id: idle_tile
                widgets:
                  - label:
                      id: idle_peer_label
                      align: CENTER
                      y: -100
                      text: "Home Assistant"
                      text_color: 0xE6EDF3
                      text_font: font_title
                  - button:
                      id: btn_prev
                      align: BOTTOM_LEFT
                      x: 10
                      y: -80
                      width: 110
                      height: 60
                      bg_color: 0x21262D
                      radius: 10
                      on_click:
                        - button.press: prev_contact_button
                      widgets:
                        - label:
                            align: CENTER
                            text: "< Пред"
                            text_color: 0xE6EDF3
                            text_font: font_btn
                  - button:
                      id: btn_call
                      align: BOTTOM_MID
                      y: -80
                      width: 160
                      height: 70
                      bg_color: 0x2ECC71
                      radius: 12
                      on_click:
                        - button.press: call_button
                      widgets:
                        - label:
                            align: CENTER
                            text: "ВЫЗОВ"
                            text_color: 0x0D1117
                            text_font: font_btn
                  - button:
                      id: btn_next
                      align: BOTTOM_RIGHT
                      x: -10
                      y: -80
                      width: 110
                      height: 60
                      bg_color: 0x21262D
                      radius: 10
                      on_click:
                        - button.press: next_contact_button
                      widgets:
                        - label:
                            align: CENTER
                            text: "След >"
                            text_color: 0xE6EDF3
                            text_font: font_btn
                  - label:
                      align: BOTTOM_MID
                      y: -20
                      text: "Нажмите для голосового"
                      text_color: 0x484F58
                      text_font: font_text

              - row: 0
                column: 1
                dir: HOR
                id: weather_tile
                widgets:
                  - label:
                      id: weather_icon_label
                      align: TOP_MID
                      y: 10
                      text: "\U000F0590"
                      text_color: 0xFFD700
                      text_font: font_icon
                  - label:
                      id: weather_temp_label
                      align: TOP_MID
                      y: 70
                      text: "--\u00B0C"
                      text_color: 0xE6EDF3
                      text_font: font_state
                  - label:
                      id: weather_condition_label
                      align: TOP_MID
                      y: 115
                      text: ""
                      text_color: 0x8B949E
                      text_font: font_text
                  - label:
                      id: weather_details_label
                      align: TOP_MID
                      y: 140
                      text: ""
                      text_color: 0x8B949E
                      text_font: font_text

                  # 5-day forecast row
                  - obj:
                      id: forecast_container
                      align: TOP_MID
                      y: 180
                      width: 460
                      height: 240
                      bg_opa: TRANSP
                      layout:
                        type: FLEX
                        flex_direction: ROW
                        flex_main_place: SPACE_AROUND
                        flex_cross_place: START
                      widgets:
                        - obj:
                            width: 84
                            height: 240
                            bg_opa: TRANSP
                            layout:
                              type: FLEX
                              flex_direction: COLUMN
                              flex_cross_place: CENTER
                              flex_row_pad: 8
                            widgets:
                              - label:
                                  id: fc0_day_label
                                  text_color: 0x2ECC71
                                  text_font: font_btn
                                  text: ""
                              - label:
                                  id: fc0_icon_label
                                  text_color: 0xFFD700
                                  text_font: font_icon_sm
                                  text: ""
                              - label:
                                  id: fc0_max_label
                                  text_color: 0xFF6B6B
                                  text_font: font_text
                                  text: ""
                              - label:
                                  id: fc0_min_label
                                  text_color: 0x6BA3FF
                                  text_font: font_text
                                  text: ""
                        - obj:
                            width: 84
                            height: 240
                            bg_opa: TRANSP
                            layout:
                              type: FLEX
                              flex_direction: COLUMN
                              flex_cross_place: CENTER
                              flex_row_pad: 8
                            widgets:
                              - label:
                                  id: fc1_day_label
                                  text_color: 0x2ECC71
                                  text_font: font_btn
                                  text: ""
                              - label:
                                  id: fc1_icon_label
                                  text_color: 0xFFD700
                                  text_font: font_icon_sm
                                  text: ""
                              - label:
                                  id: fc1_max_label
                                  text_color: 0xFF6B6B
                                  text_font: font_text
                                  text: ""
                              - label:
                                  id: fc1_min_label
                                  text_color: 0x6BA3FF
                                  text_font: font_text
                                  text: ""
                        - obj:
                            width: 84
                            height: 240
                            bg_opa: TRANSP
                            layout:
                              type: FLEX
                              flex_direction: COLUMN
                              flex_cross_place: CENTER
                              flex_row_pad: 8
                            widgets:
                              - label:
                                  id: fc2_day_label
                                  text_color: 0x2ECC71
                                  text_font: font_btn
                                  text: ""
                              - label:
                                  id: fc2_icon_label
                                  text_color: 0xFFD700
                                  text_font: font_icon_sm
                                  text: ""
                              - label:
                                  id: fc2_max_label
                                  text_color: 0xFF6B6B
                                  text_font: font_text
                                  text: ""
                              - label:
                                  id: fc2_min_label
                                  text_color: 0x6BA3FF
                                  text_font: font_text
                                  text: ""
                        - obj:
                            width: 84
                            height: 240
                            bg_opa: TRANSP
                            layout:
                              type: FLEX
                              flex_direction: COLUMN
                              flex_cross_place: CENTER
                              flex_row_pad: 8
                            widgets:
                              - label:
                                  id: fc3_day_label
                                  text_color: 0x2ECC71
                                  text_font: font_btn
                                  text: ""
                              - label:
                                  id: fc3_icon_label
                                  text_color: 0xFFD700
                                  text_font: font_icon_sm
                                  text: ""
                              - label:
                                  id: fc3_max_label
                                  text_color: 0xFF6B6B
                                  text_font: font_text
                                  text: ""
                              - label:
                                  id: fc3_min_label
                                  text_color: 0x6BA3FF
                                  text_font: font_text
                                  text: ""
                        - obj:
                            width: 84
                            height: 240
                            bg_opa: TRANSP
                            layout:
                              type: FLEX
                              flex_direction: COLUMN
                              flex_cross_place: CENTER
                              flex_row_pad: 8
                            widgets:
                              - label:
                                  id: fc4_day_label
                                  text_color: 0x2ECC71
                                  text_font: font_btn
                                  text: ""
                              - label:
                                  id: fc4_icon_label
                                  text_color: 0xFFD700
                                  text_font: font_icon_sm
                                  text: ""
                              - label:
                                  id: fc4_max_label
                                  text_color: 0xFF6B6B
                                  text_font: font_text
                                  text: ""
                              - label:
                                  id: fc4_min_label
                                  text_color: 0x6BA3FF
                                  text_font: font_text
                                  text: ""
```

- [ ] **Step 2: Verify config compiles**

Expected: Should compile. The tileview and top_bar will render on the display.

- [ ] **Step 3: Commit**

```bash
git add waveshare-s3-touch-4b-va-intercom.yaml
git commit -m "feat(4b): add top_bar and tileview with idle + weather tiles"
```

---

## Task 5: Add VA groups to main_page

**Files:**
- Modify: `waveshare-s3-touch-4b-va-intercom.yaml` — append VA groups after tileview in main_page

- [ ] **Step 1: Add VA groups as widgets inside main_page, after the tileview block**

Append these inside `main_page.widgets` (after the tileview's closing block, before `main_page` closes):

```yaml
        # === VA: LISTENING ===
        - obj:
            id: va_listening_group
            width: 480
            height: 440
            align: TOP_MID
            y: 40
            bg_opa: TRANSP
            hidden: true
            on_click:
              - voice_assistant.stop:
            widgets:
              - label:
                  align: CENTER
                  y: -40
                  text: "СЛУШАЮ..."
                  text_color: 0x2ECC71
                  text_font: font_state
              - label:
                  align: BOTTOM_MID
                  y: -30
                  text: "Нажмите чтобы остановить"
                  text_color: 0x484F58
                  text_font: font_text

        # === VA: THINKING ===
        - obj:
            id: va_thinking_group
            width: 480
            height: 440
            align: TOP_MID
            y: 40
            bg_opa: TRANSP
            hidden: true
            on_click:
              - voice_assistant.stop:
            widgets:
              - label:
                  id: thinking_text_label
                  align: TOP_MID
                  y: 40
                  width: 420
                  text: ""
                  text_color: 0xE6EDF3
                  text_font: font_text
                  long_mode: WRAP
              - label:
                  align: CENTER
                  y: 20
                  text: "ДУМАЮ..."
                  text_color: 0xF39C12
                  text_font: font_state
              - label:
                  align: BOTTOM_MID
                  y: -30
                  text: "Нажмите чтобы остановить"
                  text_color: 0x484F58
                  text_font: font_text

        # === VA: REPLYING ===
        - obj:
            id: va_replying_group
            width: 480
            height: 440
            align: TOP_MID
            y: 40
            bg_opa: TRANSP
            hidden: true
            on_click:
              - voice_assistant.stop:
            widgets:
              - label:
                  id: replying_text_label
                  align: TOP_MID
                  y: 40
                  width: 420
                  text: ""
                  text_color: 0xE6EDF3
                  text_font: font_text
                  long_mode: WRAP
              - label:
                  align: CENTER
                  y: 20
                  text: "ОТВЕЧАЮ"
                  text_color: 0x58A6FF
                  text_font: font_state
              - label:
                  align: BOTTOM_MID
                  y: -30
                  text: "Нажмите чтобы остановить"
                  text_color: 0x484F58
                  text_font: font_text

        # === VA: ERROR ===
        - obj:
            id: va_error_group
            width: 480
            height: 440
            align: TOP_MID
            y: 40
            bg_opa: TRANSP
            hidden: true
            on_click:
              - voice_assistant.start:
            widgets:
              - label:
                  align: CENTER
                  text: "ОШИБКА"
                  text_color: 0xE74C3C
                  text_font: font_state
              - label:
                  align: BOTTOM_MID
                  y: -30
                  text: "Нажмите чтобы повторить"
                  text_color: 0x484F58
                  text_font: font_text

        # === VA: MUTED ===
        - obj:
            id: va_muted_group
            width: 480
            height: 440
            align: TOP_MID
            y: 40
            bg_opa: TRANSP
            hidden: true
            widgets:
              - label:
                  align: CENTER
                  y: -40
                  text: "МИКРОФОН ВЫКЛ"
                  text_color: 0xE74C3C
                  text_font: font_state
              - label:
                  align: CENTER
                  y: 10
                  text: "Нажмите чтобы включить"
                  text_color: 0x484F58
                  text_font: font_text
              - button:
                  align: CENTER
                  y: 60
                  width: 200
                  height: 60
                  bg_color: 0x2ECC71
                  radius: 10
                  on_click:
                    - switch.turn_off: mute
                  widgets:
                    - label:
                        align: CENTER
                        text: "ВКЛЮЧИТЬ"
                        text_color: 0x0D1117
                        text_font: font_btn
```

- [ ] **Step 2: Verify config compiles**

- [ ] **Step 3: Commit**

```bash
git add waveshare-s3-touch-4b-va-intercom.yaml
git commit -m "feat(4b): add VA state groups (listening, thinking, replying, error, muted)"
```

---

## Task 6: Add intercom groups to main_page

**Files:**
- Modify: `waveshare-s3-touch-4b-va-intercom.yaml` — append IC groups after VA groups

- [ ] **Step 1: Add IC groups inside main_page.widgets**

Append after the VA groups:

```yaml
        # === IC: INCOMING CALL ===
        - obj:
            id: ic_ringing_in_group
            width: 480
            height: 440
            align: TOP_MID
            y: 40
            bg_color: 0x3D0000
            hidden: true
            widgets:
              - label:
                  align: TOP_MID
                  y: 30
                  text: "ВХОДЯЩИЙ ЗВОНОК"
                  text_color: 0xFF4444
                  text_font: font_title
              - label:
                  align: TOP_MID
                  y: 80
                  text: "от:"
                  text_color: 0xCCCCCC
                  text_font: font_text
              - label:
                  id: ic_ringing_in_peer_label
                  align: TOP_MID
                  y: 110
                  text: ""
                  text_color: 0xE6EDF3
                  text_font: font_state
              - button:
                  align: BOTTOM_LEFT
                  x: 20
                  y: -40
                  width: 200
                  height: 70
                  bg_color: 0x2ECC71
                  radius: 12
                  on_click:
                    - intercom_api.answer_call: intercom
                  widgets:
                    - label:
                        align: CENTER
                        text: "ОТВЕТИТЬ"
                        text_color: 0x0D1117
                        text_font: font_btn
              - button:
                  align: BOTTOM_RIGHT
                  x: -20
                  y: -40
                  width: 200
                  height: 70
                  bg_color: 0xE74C3C
                  radius: 12
                  on_click:
                    - button.press: decline_button
                  widgets:
                    - label:
                        align: CENTER
                        text: "ОТКЛОНИТЬ"
                        text_color: 0xE6EDF3
                        text_font: font_btn

        # === IC: OUTGOING CALL ===
        - obj:
            id: ic_outgoing_group
            width: 480
            height: 440
            align: TOP_MID
            y: 40
            bg_color: 0x1A1000
            hidden: true
            widgets:
              - label:
                  align: TOP_MID
                  y: 40
                  text: "ЗВОНЮ..."
                  text_color: 0xF39C12
                  text_font: font_title
              - label:
                  id: ic_outgoing_peer_label
                  align: TOP_MID
                  y: 100
                  text: ""
                  text_color: 0xE6EDF3
                  text_font: font_state
              - label:
                  align: TOP_MID
                  y: 150
                  text: "Ожидание ответа..."
                  text_color: 0xCCCCCC
                  text_font: font_text
              - button:
                  align: BOTTOM_MID
                  y: -40
                  width: 200
                  height: 70
                  bg_color: 0xE74C3C
                  radius: 12
                  on_click:
                    - button.press: call_button
                  widgets:
                    - label:
                        align: CENTER
                        text: "ОТМЕНА"
                        text_color: 0xE6EDF3
                        text_font: font_btn

        # === IC: IN CALL ===
        - obj:
            id: ic_in_call_group
            width: 480
            height: 440
            align: TOP_MID
            y: 40
            bg_color: 0x002200
            hidden: true
            widgets:
              - label:
                  align: TOP_MID
                  y: 40
                  text: "РАЗГОВОР"
                  text_color: 0x2ECC71
                  text_font: font_title
              - label:
                  align: TOP_MID
                  y: 80
                  text: "с:"
                  text_color: 0xCCCCCC
                  text_font: font_text
              - label:
                  id: ic_in_call_peer_label
                  align: TOP_MID
                  y: 110
                  text: ""
                  text_color: 0xE6EDF3
                  text_font: font_state
              - button:
                  align: BOTTOM_MID
                  y: -40
                  width: 200
                  height: 70
                  bg_color: 0xE74C3C
                  radius: 12
                  on_click:
                    - button.press: call_button
                  widgets:
                    - label:
                        align: CENTER
                        text: "ЗАВЕРШИТЬ"
                        text_color: 0xE6EDF3
                        text_font: font_btn
```

- [ ] **Step 2: Verify config compiles**

- [ ] **Step 3: Commit**

```bash
git add waveshare-s3-touch-4b-va-intercom.yaml
git commit -m "feat(4b): add intercom groups (incoming, outgoing, in call)"
```

---

## Task 7: Add `draw_display` script and wire up state machine

**Files:**
- Modify: `waveshare-s3-touch-4b-va-intercom.yaml` — add `draw_display` script, update `ui_state_changed`, `ui_tts_started`, `ui_stt_ended`, `ui_ic_call`

- [ ] **Step 1: Add `draw_display` script**

Add to the `script:` block:
```yaml
  - id: draw_display
    mode: restart
    then:
      - lambda: |-
          lv_obj_t* active_scr = lv_disp_get_scr_act(nullptr);

          if (!wifi::global_wifi_component->is_connected()) {
            if (active_scr != id(no_wifi_page)->obj)
              lv_disp_load_scr(id(no_wifi_page)->obj);
            return;
          }
          if (!api::global_api_server->is_connected()) {
            if (active_scr != id(no_ha_page)->obj)
              lv_disp_load_scr(id(no_ha_page)->obj);
            return;
          }
          int phase = id(voice_assistant_phase);
          if (phase == ${voice_assist_not_ready_phase_id}) {
            if (active_scr != id(no_ha_page)->obj)
              lv_disp_load_scr(id(no_ha_page)->obj);
            return;
          }
          if (phase == ${voice_assist_timer_finished_phase_id}) {
            if (active_scr != id(timer_finished_page)->obj)
              lv_disp_load_scr(id(timer_finished_page)->obj);
            return;
          }

          if (active_scr != id(main_page)->obj) {
            lv_disp_load_scr(id(main_page)->obj);
            lv_obj_clear_flag(id(idle_tile), LV_OBJ_FLAG_SCROLLABLE);
            lv_obj_clear_flag(id(weather_tile), LV_OBJ_FLAG_SCROLLABLE);
          }

          bool va_idle = (phase == ${voice_assist_idle_phase_id});

          lv_obj_add_flag(id(va_listening_group), LV_OBJ_FLAG_HIDDEN);
          lv_obj_add_flag(id(va_thinking_group), LV_OBJ_FLAG_HIDDEN);
          lv_obj_add_flag(id(va_replying_group), LV_OBJ_FLAG_HIDDEN);
          lv_obj_add_flag(id(va_error_group), LV_OBJ_FLAG_HIDDEN);
          lv_obj_add_flag(id(va_muted_group), LV_OBJ_FLAG_HIDDEN);
          lv_obj_add_flag(id(ic_ringing_in_group), LV_OBJ_FLAG_HIDDEN);
          lv_obj_add_flag(id(ic_outgoing_group), LV_OBJ_FLAG_HIDDEN);
          lv_obj_add_flag(id(ic_in_call_group), LV_OBJ_FLAG_HIDDEN);

          auto ic_state = id(intercom).get_state_str();

          bool ic_active = (ic_state == "Ringing" || ic_state == "Incoming" ||
                            ic_state == "Calling" || ic_state == "Outgoing" ||
                            ic_state == "Streaming" || ic_state == "Answering");

          if (ic_active) {
            lv_obj_add_flag(id(top_tileview), LV_OBJ_FLAG_HIDDEN);
          }

          if (ic_state == "Ringing" || ic_state == "Incoming") {
            lv_label_set_text(id(ic_ringing_in_peer_label), id(peer_name).c_str());
            lv_obj_clear_flag(id(ic_ringing_in_group), LV_OBJ_FLAG_HIDDEN);
          } else if (ic_state == "Calling" || ic_state == "Outgoing") {
            lv_label_set_text(id(ic_outgoing_peer_label), id(peer_name).c_str());
            lv_obj_clear_flag(id(ic_outgoing_group), LV_OBJ_FLAG_HIDDEN);
          } else if (ic_state == "Streaming" || ic_state == "Answering") {
            lv_label_set_text(id(ic_in_call_peer_label), id(peer_name).c_str());
            lv_obj_clear_flag(id(ic_in_call_group), LV_OBJ_FLAG_HIDDEN);
          } else if (va_idle) {
            if (!ic_active) {
              lv_obj_clear_flag(id(top_tileview), LV_OBJ_FLAG_HIDDEN);
              lv_label_set_text(id(idle_peer_label), id(peer_name).c_str());
              lv_label_set_text(id(top_bar_peer), id(peer_name).c_str());
            }
          } else if (phase == ${voice_assist_listening_phase_id}) {
            lv_obj_add_flag(id(top_tileview), LV_OBJ_FLAG_HIDDEN);
            lv_obj_clear_flag(id(va_listening_group), LV_OBJ_FLAG_HIDDEN);
          } else if (phase == ${voice_assist_thinking_phase_id}) {
            lv_label_set_text(id(thinking_text_label), id(text_request).state.c_str());
            lv_obj_add_flag(id(top_tileview), LV_OBJ_FLAG_HIDDEN);
            lv_obj_clear_flag(id(va_thinking_group), LV_OBJ_FLAG_HIDDEN);
          } else if (phase == ${voice_assist_replying_phase_id}) {
            lv_label_set_text(id(replying_text_label), id(text_response).state.c_str());
            lv_obj_add_flag(id(top_tileview), LV_OBJ_FLAG_HIDDEN);
            lv_obj_clear_flag(id(va_replying_group), LV_OBJ_FLAG_HIDDEN);
          } else if (phase == ${voice_assist_error_phase_id}) {
            lv_obj_add_flag(id(top_tileview), LV_OBJ_FLAG_HIDDEN);
            lv_obj_clear_flag(id(va_error_group), LV_OBJ_FLAG_HIDDEN);
          } else if (phase == ${voice_assist_muted_phase_id}) {
            lv_obj_add_flag(id(top_tileview), LV_OBJ_FLAG_HIDDEN);
            lv_obj_clear_flag(id(va_muted_group), LV_OBJ_FLAG_HIDDEN);
          }

      - if:
          condition:
            lambda: |-
              return id(voice_assistant_phase) == ${voice_assist_idle_phase_id} && !id(init_in_progress);
          then:
            - if:
                condition:
                  switch.is_off: mute
                then:
                  - if:
                      condition:
                        not:
                          micro_wake_word.is_running:
                      then:
                        - micro_wake_word.start:
```

- [ ] **Step 2: Update `ui_state_changed` to call `draw_display`**

Append to the existing `ui_state_changed` extension:
```yaml
  - id: !extend ui_state_changed
    then:
      - lambda: |-
          int led = id(g_applied_led);
          if (led == 0 && id(current_mode) == 1 && id(intercom).get_state_str() == "Idle") {
            id(peer_name) = id(intercom).get_current_destination();
            if (id(previous_mode) == 0) {
              id(current_mode) = 0;
            }
          }
          switch (led) {
            case 0:
              if (!api::global_api_server->is_connected()) {
                id(voice_assistant_phase) = ${voice_assist_not_ready_phase_id};
              } else if (id(mute).state) {
                id(voice_assistant_phase) = ${voice_assist_muted_phase_id};
              } else {
                id(voice_assistant_phase) = ${voice_assist_idle_phase_id};
              }
              break;
            case 1: case 2:
              id(voice_assistant_phase) = ${voice_assist_muted_phase_id};
              break;
            case 4: case 5:
              id(current_mode) = 0;
              id(voice_assistant_phase) = ${voice_assist_listening_phase_id};
              break;
            case 6:
              id(voice_assistant_phase) = ${voice_assist_thinking_phase_id};
              break;
            case 7:
              id(voice_assistant_phase) = ${voice_assist_replying_phase_id};
              break;
            case 11:
              id(voice_assistant_phase) = ${voice_assist_error_phase_id};
              break;
            default: break;
          }
      - script.execute: draw_display
```

- [ ] **Step 3: Update `ui_tts_started` to update LVGL label**

Replace the existing `ui_tts_started` extension with:
```yaml
  - id: !extend ui_tts_started
    then:
      - text_sensor.template.publish:
          id: text_response
          state: !lambda 'return text;'
      - globals.set:
          id: voice_assistant_phase
          value: '${voice_assist_replying_phase_id}'
      - script.execute: draw_display
```

- [ ] **Step 4: Update `ui_stt_ended` to update LVGL label**

Replace the empty `ui_stt_ended` with:
```yaml
  - id: !extend ui_stt_ended
    then:
      - text_sensor.template.publish:
          id: text_request
          state: !lambda 'return text;'
      - script.execute: draw_display
```

- [ ] **Step 5: Update `ui_ic_call` to trigger draw_display**

Replace existing `ui_ic_call` extension with:
```yaml
  - id: !extend ui_ic_call
    then:
      - lambda: |-
          id(peer_name) = peer;
          id(previous_mode) = id(current_mode);
          id(current_mode) = 1;
      - script.execute: draw_display
```

- [ ] **Step 6: Verify config compiles**

Expected: Full UI logic compiles.

- [ ] **Step 7: Commit**

```bash
git add waveshare-s3-touch-4b-va-intercom.yaml
git commit -m "feat(4b): add draw_display script, wire state machine to LVGL"
```

---

## Task 8: Add clock update interval and show initializing page at boot

**Files:**
- Modify: `waveshare-s3-touch-4b-va-intercom.yaml` — add `on_boot` show init page, add clock interval

- [ ] **Step 1: Add `lvgl.page.show: initializing_page` to on_boot**

In the existing `on_boot:` block at priority `-100.0` (or after API connect), add a boot show. Find the `on_boot` section at priority `-100` and add `lvgl.page.show: initializing_page` at priority `800` (after display init but before network):

```yaml
  on_boot:
    - priority: 800
      then:
        - lvgl.page.show: initializing_page
```

Note: Priority 800 runs after hardware init but before network. The `draw_display` will switch to `no_wifi_page` or `main_page` once WiFi/API connects.

- [ ] **Step 2: Add clock update interval**

Add to the `interval:` section (or create it):
```yaml
interval:
  - interval: 30s
    then:
      - lambda: |-
          auto now = id(sntp_time).now();
          if (now.is_valid()) {
            char clk[8];
            snprintf(clk, sizeof(clk), "%02d:%02d", now.hour, now.minute);
            lv_label_set_text(id(clock_label), clk);
          }
```

Note: If an `interval:` block already exists (line 215), merge this into it.

- [ ] **Step 3: Verify config compiles**

- [ ] **Step 4: Commit**

```bash
git add waveshare-s3-touch-4b-va-intercom.yaml
git commit -m "feat(4b): show initializing page at boot, add clock update interval"
```

---

## Task 9: Flash and verify on device

- [ ] **Step 1: Push to git**

```bash
git push origin main
```

- [ ] **Step 2: Clear stale package cache**

```bash
sudo rm -rf /mnt/services/esphome/esphome/.esphome/packages/852a4dc7
```

- [ ] **Step 3: Compile firmware**

```bash
docker run --rm -v /mnt/services/esphome/esphome:/config -v /mnt/services/esphome/esphome-intercom:/config/intercom ghcr.io/esphome/esphome:2026.4.3 compile esp32-s3-smart-86-box-hall.yaml
```

- [ ] **Step 4: Kill ESPHome container to free serial port**

```bash
docker kill esphome
```

- [ ] **Step 5: Flash firmware**

```bash
sudo PYTHONPATH=/home/lacitis/.local/lib/python3.12/site-packages /usr/local/bin/python3.12 -m esptool --chip esp32s3 --port /dev/ttyACM0 --baud 460800 --before usb_reset --after hard_reset write_flash 0x0 /mnt/services/esphome/esphome/.esphome/build/esp32-s3-smart-86-box-hall/.pioenvs/esp32-s3-smart-86-box-hall/firmware.factory.bin
```

- [ ] **Step 6: Verify on device**

Expected behavior:
1. Boot → "Загрузка..." on black screen
2. WiFi connects → switches to no_ha_page or main_page
3. API connects → main_page with idle tile (peer name + buttons)
4. Swipe right → weather tile
5. BOOT button → VA listening screen
6. Touch "ВЫЗОВ" → intercom outgoing screen

- [ ] **Step 7: Commit any hot-fixes**

```bash
git add waveshare-s3-touch-4b-va-intercom.yaml
git commit -m "fix(4b): UI hot-fixes from device testing"
```
